@model QuanLyNhanSu.ViewModels.Employee.EditEmployeeViewModel
@using QuanLyNhanSu.Commons
@{
    ViewData["Title"] = "Edit";
}

@{
    var resultHotenNv = (ValidateResult)ViewBag.resultHotenNv;
    var resultGioitinh = (ValidateResult)ViewBag.resultGioitinh;
    var resultQueQuan = (ValidateResult)ViewBag.resultQueQuan;
    var resultSđt = (ValidateResult)ViewBag.resultSđt;
    var resultSoCmtnd = (ValidateResult)ViewBag.resultSoCmtnd;
    var resultĐiachithuongtru = (ValidateResult)ViewBag.resultĐiachithuongtru;
}

<br />
<h3>Hồ sơ cá nhân</h3>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit" method="post">
            <div class="form-group">
                <input asp-for="Msnv" class="form-control"  type="hidden"/>
            </div>
            <div class="form-group">
                <label asp-for="HotenNv" class="control-label"></label>
                <input asp-for="HotenNv" class="form-control" />
                @if (resultHotenNv != null)
                {
                    @if (resultHotenNv._isNull == false)
                    {
                        <span class="text-danger">@resultHotenNv._message</span>
                    }
                }
            </div>
            <div class="form-group">
                <label asp-for="Gioitinh" class="control-label"></label>
                <input asp-for="Gioitinh" class="form-control" />
                @if (resultGioitinh != null)
                {
                    @if (resultGioitinh._isNull == false)
                    {
                        <span class="text-danger">@resultGioitinh._message</span>
                    }
                }
            </div>
            <div class="form-group">
                <label asp-for="Ngaysinh" class="control-label"></label>
                <input asp-for="Ngaysinh" class="form-control" />
               
            </div>
            <div class="form-group">
                <label asp-for="QueQuan" class="control-label"></label>
                <input asp-for="QueQuan" class="form-control" />
                @if (resultQueQuan != null)
                {
                    @if (resultQueQuan._isNull == false)
                    {
                        <span class="text-danger">@resultQueQuan._message</span>
                    }
                }
            </div>
            <div class="form-group">
                <label asp-for="Sđt" class="control-label"></label>
                <input asp-for="Sđt" class="form-control" />
                @if (resultSđt != null)
                {
                    @if (resultSđt._isNull == false)
                    {
                        <span class="text-danger">@resultSđt._message</span>
                    }
                }
            </div>
            <div class="form-group">
                <label asp-for="SoCmtnd" class="control-label"></label>
                <input asp-for="SoCmtnd" class="form-control" />
                @if (resultSoCmtnd != null)
                {
                    @if (resultSoCmtnd._isNull == false)
                    {
                        <span class="text-danger">@resultSoCmtnd._message</span>
                    }
                }
            </div>
            <div class="form-group">
                <label asp-for="Ngaycap" class="control-label"></label>
                <input asp-for="Ngaycap" class="form-control" />
            </div>
            <div class="form-group">
                <label asp-for="Noicap" class="control-label"></label>
                <input asp-for="Noicap" class="form-control" />

            </div>
            <div class="form-group">
                <label asp-for="Điachithuongtru" class="control-label"></label>
                <input asp-for="Điachithuongtru" class="form-control" />
                @if (resultĐiachithuongtru != null)
                {
                    @if (resultĐiachithuongtru._isNull == false)
                    {
                        <span class="text-danger">@resultĐiachithuongtru._message</span>
                    }
                }
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

